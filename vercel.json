{
  "version": 2,
  "buildCommand": "npm install && ./node_modules/.bin/tsc || tsc",
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.ts"
    }
  ]
}